def add_numbers(x: int, c: int) -> int:
    """Add two numbers together.

    Args:a
      x (int): a number
      y (int): a number

    Returns:
      (int). The sum of the two numbers.
    """
    return x + c


def subtract_numbers(a: int, b: int) -> int:
    """Subtract two numbers together.

    Args:a
      x (int): a number
      y (int): a number

    Returns:
      (int). The subtraction of the two numbers.
    """
    return a - b
